# Local Python scratch.
.venv/
__pycache__/
*.pyc
.ipynb_checkpoints/

# Local/private evaluation artifacts are not part of the plug-and-play model package.
data/
reports/
errors/
reviews/
loop_state.json

# Keep accidental extra checkpoints out of Git. The shipped fine-tuned checkpoint is already tracked
# under this directory via explicit Git LFS entries.
model-finetuned/

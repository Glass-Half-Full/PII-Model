# Local PII & GIRP classification — dependencies.
# Target: Python 3.13 on Windows (also works on macOS/Linux), CPU or NVIDIA GPU.
# Install (no venv/conda) via pip / your internal mirror:
#     python -m pip install -r requirements.txt
#
# torch + transformers are pulled in automatically by gliner2.
# For NVIDIA GPU acceleration, ensure a CUDA build of torch is available from your internal
# mirror; otherwise the tool runs on CPU automatically. No external downloads are required at
# run time — the model loads entirely from local files.
gliner2>=1.3.1
pandas>=2.0
tqdm>=4.66

# Australian hybrid PII extras (for aupii.py) — install ON TOP of requirements.txt:
#     python -m pip install -r requirements.txt -r requirements-hybrid.txt
#
# Adds Microsoft Presidio checksum/regex recognizers + Australian TFN/Medicare/ABN/ACN.
# No spaCy language-model download is needed; aupii.py supplies a no-op NLP engine because the
# hybrid uses Presidio for pattern/checksum recognition and GLiNER2 for NER.
presidio-analyzer>=2.2
